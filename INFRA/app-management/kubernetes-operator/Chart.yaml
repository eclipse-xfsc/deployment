# Chart.yaml
apiVersion: v2
name: kubernetes-operator
description: Operator for managing the dependencies between resource providers and consumers.
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: kubernetes-operator
    version: ">=0.1.0"
    repository: oci://node-654e3bca7fbeeed18f81d7c7.de-west-1.xaas.plus.cloud/common-services/kubernetes-operator 