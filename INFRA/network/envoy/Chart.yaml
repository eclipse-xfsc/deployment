apiVersion: v2
name: xfsc-envoy-gateway
description: Shared Envoy Gateway for XFSC tenant ListenerSets
type: application
version: 0.2.0
appVersion: "1.8.3"
dependencies:
  - name: gateway-helm
    alias: envoy-gateway
    version: "1.8.3"
    repository: "oci://docker.io/envoyproxy"
    condition: envoy-gateway.enabled
