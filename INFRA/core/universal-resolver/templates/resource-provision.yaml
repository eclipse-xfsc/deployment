apiVersion: resources.xfsc.io/v1alpha1
kind: ResourceProvider
metadata:
  name: did-resolver
spec:
  type: service
  outputs:
    env:
     DID_RESOLVER: {{ printf "http://%s.%s.svc.cluster.local" (include "universal-resolver.serviceName" .) .Release.Namespace | quote }}