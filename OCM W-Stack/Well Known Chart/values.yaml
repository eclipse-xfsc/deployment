well-known-service:
  image: 
    tag: 1.4.0
    pullPolicy: Always
  podAnnotations: 
    inject.xfsc.io/enabled: "true"
  
  config:
    loglevel: DEBUG
    isdev: false
    importer:
      broadcast:
