issuance-service:
  image:
    pullPolicy: Always
    tag: 1.9.1-oidvci10

  ingress:
   enabled: false

  podAnnotations: 
    inject.xfsc.io/enabled: "true"

  config:
    logLevel: info
    servingPort: 8080
