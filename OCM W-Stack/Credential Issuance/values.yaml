issuance-service:
  image:
    pullPolicy: Always
    tag: 1.6.2

  ingress:
   enabled: false

  podAnnotations: 
    inject.xfsc.io/enabled: "true"

  config:
    logLevel: info
    servingPort: 8080
