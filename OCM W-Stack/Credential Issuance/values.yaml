issuance-service:
  image:
    pullPolicy: Always

  ingress:
   enabled: false

  podAnnotations: 
    inject.xfsc.io/enabled: "true"

  config:
    audience: https://cloud-wallet.DOMAIN
    logLevel: info
    servingPort: 8080
