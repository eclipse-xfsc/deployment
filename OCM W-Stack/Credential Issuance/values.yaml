issuance-service:
  image:
    pullPolicy: Always
    tag: 1.4.7

  ingress:
   enabled: false

  podAnnotations: 
    inject.xfsc.io/enabled: "true"

  config:
    audience: https://cloud-wallet.DOMAIN
    logLevel: info
    servingPort: 8080
