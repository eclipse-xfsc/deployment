signer:
  useConfigMap: false
  configMapName: my-configmap
  useSecretRef: false
  secretRefName: my-secret
  signer: 
    vault:
      secret: vault
  podAnnotations: 
    inject.xfsc.io/enabled: "true"
